/**
 * This is a marker interface to allow a consistent view of data exchange between client and server.
 * In the application business logic, before working with references of the type DataPackage, it is important to cast
 * the references to the concrete type being used for the application, so as to extract the fields for manipulation
 */
package com.datapackagecommon.interfaces;

/**
 * @author Adrian Nwajuobi
 *
 */
public interface DataPackage {
	
}
